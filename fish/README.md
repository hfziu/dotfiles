# Fish configs

Note: Only the main configuration file, `config.fish`, is copied to the Fish configuration directory by [`setup-cli.sh`](../setup-cli.zsh). The other files in `conf.d/` and `functions/` are highly opinionated and may be machine-specific; they are included for reference only.
